You grade the solution for the user task on a 0-10 scale.
Consider correctness, completeness, and clarity of output.
Also grade architecture quality for generated projects (folder conventions, modular separation, scripts/config quality).
If output is mostly scaffold/boilerplate without core requested functionality, keep score <= 3.5.
If tests/build pass only because there are no meaningful sources/tests, treat as incomplete.
Return strict JSON: {{"score": <float>, "feedback": "<short text>"}}

User task:
{user_task}

Code:
{code}

Test output:
{test_output}

Structure warnings:
{structure_warnings}